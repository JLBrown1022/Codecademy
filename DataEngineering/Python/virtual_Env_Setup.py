# From a terminal, run the following command: pip --version
# Install pipenv: pip install --user pipenv

#  WARNING: The script virtualenv is installed in '/Users/jameslbrown/.local/bin' which is not on PATH.
# Consider adding this directory to PATH or, if you prefer to suppress this warning, use --no-warn-script-location.
# WARNING: The script pipenv is installed in '/Users/jameslbrown/.local/bin' which is not on PATH.
# To fix this warning, add the following line to your .bash_profile file: export PATH=$PATH:/Users/jameslbrown/.local/bin
#
#  Run the following command: vi ~/.bash_profile
# (enter) i (to insert text into the file)
# (type) export PATH="/Users/jameslbrown/.local/bin:$PATH"
# (esc) :wq! (write and quit)







